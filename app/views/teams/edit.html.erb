<% if not logged_in? or not @team.users.include?(current_user) %>
	redirect_to root_path
<% else %>
<!--
<strong> <%= @team.users.include?(current_user) %> </strong>
-->

<%= render(
	partial: 'teams/list'
) %>
 
<%= link_to 'Назад', team_path, :class => 'btn btn-primary' %> 

<% end %>
 
